# Решение проблем

## Проблема: Страница не открывается или показывает ошибку

### Шаг 1: Проверьте, что оба сервера запущены

**Backend (порт 3000):**
```bash
# В корне проекта
npm run dev
```

Должно быть:
```
🚀 Сервер запущен на порту 3000
📚 Школьный портал готов к работе!
```

**Frontend (порт 5173):**
```bash
# В папке frontend
cd frontend
npm run dev
```

Должно быть:
```
VITE v7.2.2 ready in XXX ms
Local: http://localhost:5173/
```

### Шаг 2: Проверьте Backend API

**⚠️ ВАЖНО:** НЕ открывайте `http://localhost:3000/api/` - такого маршрута нет!

Откройте в браузере:
```
http://localhost:3000/api/health
```

Должен вернуться:
```json
{"status":"ok","message":"Школьный портал работает!"}
```

**Почему `/api/` не работает?**
- `/api/` - это не существующий маршрут
- Все маршруты имеют конкретные пути: `/api/health`, `/api/auth/login`, и т.д.
- Это нормально, что `/api/` возвращает 404!

Если не работает:
- Проверьте, что порт 3000 свободен: `netstat -ano | findstr :3000`
- Остановите процесс: `npm run stop`

### Шаг 3: Создайте файл .env в папке frontend

Создайте файл `frontend/.env`:
```env
VITE_API_URL=http://localhost:3000/api
```

**Важно:** После создания .env файла перезапустите frontend сервер!

### Шаг 4: Откройте приложение правильно

1. Откройте браузер (Chrome, Firefox, Edge)
2. В **адресной строке** (не в поиске!) введите:
   ```
   http://localhost:5173
   ```
3. Нажмите Enter

### Шаг 5: Проверьте консоль браузера

1. Нажмите F12 в браузере
2. Откройте вкладку "Console"
3. Проверьте, есть ли ошибки (красным цветом)

### Шаг 6: Проверьте Network запросы

1. В консоли браузера (F12) откройте вкладку "Network"
2. Попробуйте зарегистрироваться или войти
3. Проверьте, есть ли запросы к `http://localhost:3000/api`

## Частые ошибки

### Ошибка: "Failed to fetch" или "Network Error"

**Причина:** Frontend не может подключиться к Backend

**Решение:**
1. Проверьте, что backend запущен на порту 3000
2. Проверьте файл `.env` в папке `frontend`
3. Убедитесь, что в `.env` указано: `VITE_API_URL=http://localhost:3000/api`
4. Перезапустите frontend сервер

### Ошибка: "Cannot GET /"

**Причина:** Неправильный роутинг

**Решение:**
- Убедитесь, что открываете `http://localhost:5173` (не `/api/health`)
- Попробуйте открыть `http://localhost:5173/login` напрямую

### Ошибка: "401 Unauthorized"

**Причина:** Токен истек или недействителен

**Решение:**
- Очистите localStorage в браузере (F12 → Application → Local Storage → очистить)
- Попробуйте зарегистрироваться заново

### Страница белая/пустая

**Причина:** Ошибка JavaScript

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки
3. Убедитесь, что все зависимости установлены: `npm install --ignore-scripts`

## Проверка работы по шагам

1. ✅ Backend запущен на порту 3000
2. ✅ Frontend запущен на порту 5173
3. ✅ Файл `.env` создан в папке `frontend`
4. ✅ `http://localhost:3000/api/health` возвращает JSON
5. ✅ `http://localhost:5173` открывается в браузере
6. ✅ В консоли браузера нет ошибок

Если все шаги выполнены, но проблема остается - пришлите скриншот консоли браузера (F12 → Console).

